# Passwords for the ClickHouse users used by Vector (RW) and Grafana (RO).
# These must match the values provisioned on the ClickHouse VM — see
# `new-infra/infrastructure/05-clickhouse/.envrc`
# (TF_VAR_vector_password, TF_VAR_grafana_ro_password).
#
# Fill in base64-encoded values before committing:
#   echo -n "$TF_VAR_vector_password"     | base64
#   echo -n "$TF_VAR_grafana_ro_password" | base64
apiVersion: v1
kind: Secret
metadata:
  name: clickhouse-credentials
  namespace: logging
type: Opaque
data:
  vector-password: ZFRUVjBnR3lZbGJ6WVlEUA==
  grafana-ro-password: TmZqR1kwUjJUUlRxeUdRVQ==
